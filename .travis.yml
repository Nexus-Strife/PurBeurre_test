language: python

python:
  - "3.6"
  
before_script:
  - pip install -r requirements.txt
  - psql -c 'create database pur_beurre;' -U nex -P C1li2tn45

env: DJANGO_SETTINGS_MODULE="pur_beurre.settings"

services:
  - postgresql

script:
  - python3 manage.py test
