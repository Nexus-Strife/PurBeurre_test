language: python

python:
  - "3.6"
  
before_script:
  - pip install -r requirements.txt

env: DJANGO_SETTINGS_MODULE="pur_beurre.settings"

services:
  - postgresql

script:
  - python3 manage.py test
