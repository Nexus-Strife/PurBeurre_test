language: python

python:
  - "3.6"
  
install: 
  - pip3 install pipenv
  - pipenv install 
  
before_script:
  - pip install -r requirements.txt

env: DJANGO_SETTINGS_MODULE="pur_beurre.settings"

services:
  - postgresql

script:
  - ./manage.py test
